// export default function random() {
//   return Math.floor(Math.random() * 10)
// }

export function random() {
  return Math.floor(Math.random() * 10)
}

export const user = {
  name: 'Jongseo',
  age: 40
}

// 기본 통로는 한 개의 함수만 내보낼 수 있지만
// 이름 지정 내보내기는 여러 개의 함수를 내보낼 수 있음

export default 123 

// 두 통로를 동시에 사용할 수도 있음